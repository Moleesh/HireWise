/** @format */

import PageHero from '../../shared/components/PageHero';

/** DashboardHero - Prominent dashboard heading for small and large screens. */
const DashboardHero = () => (
	<PageHero
		eyebrow="Hiring Overview"
		title="Dashboard"
		description="A quick view of open roles, candidate activity, and ranking progress."
	/>
);

export default DashboardHero;
